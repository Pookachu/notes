The linearization of f(x) = cot x at x =a is given by L(x) = f(a)+f'(a)(x-a)

2. Use the approximation $(1+x)^k \approx 1+ kx$ to estimate the following.
	a. $(1.0005)^{50}$

	b. $\sqrt[4]{1.009}$

3. Find a linearization at a suitably chosen integer near a at which the given function and its derivative are easy to evaluate